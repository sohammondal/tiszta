export interface Colors {
    BuddhaGold: string
    Orange: string
    WestSide: string
    SuperNova: string
    Sundown: string
    BridalHealth: string
    Mongoose: string
    Mischka: string
    Black: string
}

export enum Themes {
    LIGHT = 'light',
    DARK = 'dark',
}
